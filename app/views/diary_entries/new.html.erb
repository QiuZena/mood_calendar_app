<h1>New Mood Diary</h1>

<% if DiaryEntry.exists?(user_id: current_user.id, entry_date: Date.today) %>
  <div class="alert" style="text-align: center; background-color: #fff3cd; padding: 1rem; margin-bottom: 1rem; border-radius: 8px;">
    📅 You've already posted your mood today.
    <%= link_to "Edit it here", edit_diary_entry_path(DiaryEntry.find_by(user_id: current_user.id, entry_date: Date.today)), style: "color: #0c5460; text-decoration: underline; margin-left: 8px;" %>
  </div>
<% end %>

<div class="form-wrapper">
    <%= render partial: "form", locals: { diary_entry: @diary_entry, moods: @moods } %>


  <br>

  <%= link_to 'Back', diary_entries_path, class: 'back-link' %>
</div>

